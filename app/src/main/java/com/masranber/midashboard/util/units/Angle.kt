package com.masranber.midashboard.util.units

/*class _theta : Dimension() {}

object rad : BaseUnit<_theta>() {
    override val symbol: String get() = "rad"
}

object deg : DerivedUnit<_theta> {
    override fun toBaseUnit(derived: Double): Double {
        return derived * (Math.PI/180.0)
    }

    override fun fromBaseUnit(base: Double): Double {
        return base * (180.0/Math.PI)
    }

    override val symbol: String get() = "°"
}

typealias Angle = Quantity<_theta>

val Number.rad get() = Angle(this.toDouble(), com.masranber.midashboard.domain.units.rad)
val Number.deg get() = Angle(this.toDouble(), com.masranber.midashboard.domain.units.deg)

 */